{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.NameetP/pdfmux",
  "title": "pdfmux",
  "description": "PDF-to-Markdown extraction that audits its own output and certifies any other extractor's, flagging pages they silently dropped. 7 tools incl. verify_extraction.",
  "version": "1.8.4",
  "repository": {
    "url": "https://github.com/NameetP/pdfmux",
    "source": "github"
  },
  "websiteUrl": "https://pdfmux.com",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "pdfmux",
      "version": "1.8.4",
      "transport": {
        "type": "stdio"
      },
      "runtimeHint": "pdfmux",
      "runtimeArguments": [
        {
          "type": "positional",
          "value": "serve",
          "isRequired": true
        }
      ],
      "environmentVariables": [
        {
          "name": "GEMINI_API_KEY",
          "description": "Optional Google AI API key. Enables the paid Gemini Flash fallback for pages where all local backends report low confidence. pdfmux runs 100% locally without this.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        }
      ]
    }
  ]
}
